---
- name: BUILDAH | Test "buildah pull <image_name>" command
  mafalb.buildah.buildah_pull:
    name: quay.io/ipbabble/myfedoratest
  register: result

- name: BUILDAH | Debug
  ansible.builtin.debug:
    var: result.stdout_lines
...
